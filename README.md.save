To build run "sudo docker build . -f Rodetas -t runrodetas.sh"
To run use "sudo docker run --network="host" -it runrodetas.sh /bash/binattackerRobot.getSelfState().position"
then run "runRodetas.sh yellow" or "runRodetas.sh blue"
